import {AppRegistry} from 'react-native';

__CODE__

AppRegistry.registerComponent('preview', () => __NAME__);
AppRegistry.runApplication('preview', {
  rootTag: document.getElementById('preview'),
});
