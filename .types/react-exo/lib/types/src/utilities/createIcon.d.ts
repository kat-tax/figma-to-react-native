import { StyleSheet } from 'react-native';
/**
 * Clones an icon with the given icon styles
 * @param icon The icon element to clone
 * @param styles The styles to apply to the icon
 * @returns A new react element with the given styles
 */
export declare function createIcon(icon?: React.ReactElement, styles?: StyleSheet.NamedStyles<object>): import('react').ReactElement<any, string | import('react').JSXElementConstructor<any>> | null;
