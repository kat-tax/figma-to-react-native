import { IconComponent } from './Icon.base';
/** A component that can display over 200,000 icons via [Iconify](https://icon-sets.iconify.design) */
export declare const Icon: Omit<IconComponent, 'Remote' | 'New'>;
