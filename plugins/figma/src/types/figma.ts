export type VariableModes = {
  current: {
    modeId: string;
    name: string;
  };
  default: {
    modeId: string;
    name: string;
  };
  modes: {
    modeId: string;
    name: string;
  }[];
}
