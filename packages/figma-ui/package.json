{
  "name": "figma-ui",
  "type": "module",
  "private": true,
  "files": [
    "lib"
  ],
  "exports": {
    ".": "./lib/index.js",
    "./css/*.css": "./lib/css/*.css"
  },
  "main": "./lib/index.js",
  "scripts": {
    "prebuild": "pnpm run clean",
    "build": "concurrently pnpm:build:gen-colors-types pnpm:build:gen-icons && pnpm run build:gen-index && pnpm run build:js && pnpm run build:copy-non-ts-files",
    "build:gen-colors-types": "tsx scripts/gen-colors-types.ts",
    "build:gen-icons": "tsx scripts/gen-icons.ts",
    "build:gen-index": "rimraf src/index.ts && tsx scripts/gen-index.ts -- 'src/**/*.{ts,tsx}' '!src/**/private/**/*'",
    "build:copy-non-ts-files": "tsx scripts/copy-non-ts-files.ts",
    "build:js": "tsc --build",
    "clean": "rimraf --glob '*.log' lib 'src/**/*.css.d.ts' tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "@sindresorhus/slugify": "^2.2.1",
    "@types/node": "^20.8.6",
    "camelcase": "^8.0.0",
    "concurrently": "^8.2.1",
    "globby": "^13.2.2",
    "path-exists": "^5.0.0",
    "preact": "^10.5.13",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "rimraf": "^5.0.5",
    "svgo": "^3.0.2",
    "tsx": "^3.13.0",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "react": ">=18"
  },
  "sideEffects": false
}
